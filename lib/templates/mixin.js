'use strict';

/**
 * Module exports
 */

module.exports = <%= _.classify(name) %> ;

/**
 * Module function
 */

function <%= _.classify(name) %> () {
  this.attributes({

  });

  this.after('initialize', function() {

  });
}
