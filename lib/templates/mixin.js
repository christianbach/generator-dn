'use strict';

/**
 * Module exports
 */

module.exports = with <%= _.classify(name) %> ;

/**
 * Module function
 */

function with <%= _.classify(name) %> () {
  this.attributes({

  });

  this.after('initialize', function() {

  });
}
